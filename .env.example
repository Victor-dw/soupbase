# The only feature setting: byok_only | site_only | both
AI_ACCESS_MODE=byok_only
# Server-only secret; required for site_only and both. Never commit a real key.
AI_GATEWAY_API_KEY=
# Vercel Neon integration injects this automatically. Leave empty for local PGlite.
DATABASE_URL=
